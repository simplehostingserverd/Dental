/**
 * Run `build` or `dev` with `SKIP_ENV_VALIDATION` to skip env validation. This is especially useful
 * for Docker builds.
 */
// require("./src/env.ts"); // Temporarily commented out for debugging

/** @type {import("next").NextConfig} */
const config = {};

module.exports = config;
